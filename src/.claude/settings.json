{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd /c \"if not exist project_agents\\logger mkdir project_agents\\logger && set > project_agents\\logger\\env-dump.txt\""
          }
        ]
      }
    ]
  }
}